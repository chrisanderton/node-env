{
  "name": "node-env",
  "description": "simple test app exposing environment",
  "scripts": {
    "postdeploy": "postdeploy"
  },
  "env": {
    "HEROKU_APP_NAME": {
      "required": true
    },
    "DOMAIN": {
      "required": true
    },
    "HEROKU_API_KEY": {
      "required": true
    },
    "DNS_USERNAME": {
      "required": true
    },
    "DNS_API_KEY": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
